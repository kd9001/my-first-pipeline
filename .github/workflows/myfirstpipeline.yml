
name: My App Pipeline

on:
  push:
    branches:
      - main
      - branch

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello from my pipeline"

      - name: Print message
        run: echo "Build Complete"
